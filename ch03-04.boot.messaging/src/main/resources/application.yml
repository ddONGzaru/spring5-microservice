
server:
  host: 118.47.55.168

spring:

  rabbitmq:
    addresses: ${server.host}:5672,${server.host}:5682,${server.host}:5692
    username: admin
    password: admin

  main:
    web-application-type: none


